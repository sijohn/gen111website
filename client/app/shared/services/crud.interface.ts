export interface CRUD {
    type: string;
    sortType: string;
    field: string;
    noSort: Boolean;
    noAdd: Boolean;
    noEdit: Boolean;
    options: string;
    link: string;
    heading: string;
    title: string;
}
